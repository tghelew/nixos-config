{ colors, fonts, ... }:
''
# Automatically created by Nix to not edit!!

osd-font='Netflix Sans Medium'
osd-font-size=20
osd-color='${colors.white}'               # Hex code for white
osd-border-color='${colors.black}'        # Hex code for black
osd-border-size=0.6                             # Size for osd text and progress bar
osd-blur=0.2                                    # Gaussian blur factor. 0 means no blur applied (default)

# The following options only apply to subtitles without own styling
sub-font='Netflix Sans Medium'                   # Specify font to use for subtitles that do not themselves specify a particular font
sub-font-size=40
sub-color='${colors.white}'               # Hex code for white
sub-border-color='${colors.black}'        # Hex code for black
sub-border-size=2.0
sub-shadow-offset=0
sub-spacing=0.0
''
